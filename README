AI Project 1 - Main
===================

by Carlos Ezequiel and Benjamin Geiger


To Execute:
-----------

To execute the program, run at the command line:

python main.py <locations.txt> <connections.txt> <start> <end> [<blocked> [<blocked> ...]]

If there is no valid route from the start to the end without going
through a blocked city, the program will throw an exception.
